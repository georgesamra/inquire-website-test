import React from 'react';
/*

  interface ButtonProps {
      kind: string
  }

  let bibleLink: string | undefined = undefined;





  function InquireButton(props: Readonly<ButtonProps>){
    const [data, setData] = React.useState(null);

    function GetBook() {
      React.useEffect(() => {
        fetch("/api")
          .then((res) => res.json())
          .then((data) => setData(data.message));
      }, []);
    }

    if (props.kind == "inquire") {
      return (
        <div>
          <button onClick={GetBook}>Inquire</button>
          <p>{ data }</p>
        </div>
        )
      } else {
        return (
          <button onClick={openPassage}>Bible Link</button>
        );
    }
  }


  function openPassage() {
    window.open(bibleLink, '_blank')?.focus();
    
  }

  export default InquireButton;
  */


// export default InquireButton;